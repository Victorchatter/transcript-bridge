[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "transcript-bridge"
version = "0.3.0"
description = "Loss-aware conversion between agent transcript formats"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "Victor Filtchev"}]
keywords = ["claude", "openai", "codex", "gemini", "langsmith", "langfuse", "ollama", "transcript", "agent", "jsonl"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]

[project.urls]
Homepage = "https://github.com/Victorchatter/transcript-bridge"
Repository = "https://github.com/Victorchatter/transcript-bridge"

[project.scripts]
transcript-bridge = "transcript_bridge.cli:main"

[tool.setuptools.packages.find]
include = ["transcript_bridge*"]
